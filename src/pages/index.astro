---
import Layout from '../layouts/Layout.astro';

---

<Layout title="Resources Hub">
	<main>
		<div class="hero">
			<i class="ph ph-cube-transparent hero-icon"></i>
			<h1>Bienvenido a <span class="text-gradient">Resources Hub</span></h1>
			<p class="subtitle">
				Descubra recursos seleccionados para desarrolladores, diseñadores y creadores..
			</p>
			<div class="cta-container">
				<a href="/resources" class="cta-button">
					<i class="ph ph-compass"></i>
					Navega por los recursos
				</a>
			</div>
		</div>
		<div class="features">
			<div class="feature">
				<i class="ph ph-books"></i>
				<h3>Contenido selccionado</h3>
				<p> Recursos seleccionados para ayudarte a destacar</p>
			</div>
			<div class="feature">
				<i class="ph ph-folders"></i>
				<h3>Categorias organizadas</h3>
				<p>Colecciones de recursos de fácil navegación
				</p>
			</div>
			<div class="feature">
				<i class="ph ph-lightning"></i>
				<h3>Acceso rapido</h3>
				<p>Enlaces directos a recursos valiosos
				</p>
			</div>
		</div>
	</main>
</Layout>

<style>
	main {
		margin: auto;
		padding: 1rem;
		width: 800px;
		max-width: calc(100% - 2rem);
		color: var(--text);
		font-size: 20px;
		line-height: 1.6;
	}
	.hero {
		text-align: center;
		padding: 4rem 0;
	}
	.hero-icon {
		font-size: 4rem;
		color: var(--accent);
		margin-bottom: 1rem;
	}
	h1 {
		font-size: 3.5rem;
		font-weight: 700;
		line-height: 1.2;
		margin-bottom: 1rem;
	}
	.text-gradient {
		background: linear-gradient(45deg, var(--accent), var(--secondary));
		-webkit-background-clip: text;
		-webkit-text-fill-color: transparent;
		background-size: 400%;
		background-position: 0%;
	}
	.subtitle {
		color: var(--text);
		opacity: 0.9;
		font-size: 1.25rem;
		margin-bottom: 2rem;
	}
	.cta-container {
		margin-top: 2rem;
	}
	.cta-button {
		display: inline-flex;
		align-items: center;
		gap: 0.5rem;
		padding: 1rem 2rem;
		background: var(--accent);
		color: var(--primary);
		border-radius: 8px;
		text-decoration: none;
		font-weight: 600;
		transition: all 0.3s ease;
		font-size: 1.125rem;
	}
	.cta-button:hover {
		transform: translateY(-2px);
		box-shadow: 0 4px 12px rgba(246, 177, 122, 0.3);
	}
	.features {
		display: grid;
		grid-template-columns: repeat(3, 1fr);
		gap: 2rem;
		margin-top: 4rem;
		padding: 2rem;
		background: var(--primary);
		border-radius: 12px;
	}
	.feature {
		text-align: center;
		padding: 1.5rem;
		background: var(--primary-light);
		border-radius: 8px;
		transition: all 0.3s ease;
	}
	.feature:hover {
		transform: translateY(-4px);
	}
	.feature i {
		font-size: 2.5rem;
		color: var(--accent);
		margin-bottom: 1rem;
	}
	.feature h3 {
		font-size: 1.25rem;
		margin-bottom: 0.5rem;
	}
	.feature p {
		font-size: 0.95rem;
		opacity: 0.9;
		margin: 0;
	}
</style>